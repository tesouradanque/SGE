$(document).ready(function(){

	if(jQuery('#bsDatepicker').length > 0 ){
		$('#bsDatepicker').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker1').length > 0 ){
		$('#bsDatepicker1').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker2').length > 0 ){
		$('#bsDatepicker2').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker3').length > 0 ){
		$('#bsDatepicker3').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker4').length > 0 ){
		$('#bsDatepicker4').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker5').length > 0 ){
		$('#bsDatepicker5').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}
	
	if(jQuery('#bsDatepicker6').length > 0 ){
		$('#bsDatepicker6').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker7').length > 0 ){
		$('#bsDatepicker7').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker8').length > 0 ){
		$('#bsDatepicker8').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker9').length > 0 ){
		$('#bsDatepicker9').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker10').length > 0 ){
		$('#bsDatepicker10').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker11').length > 0 ){
		$('#bsDatepicker11').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker12').length > 0 ){
		$('#bsDatepicker12').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker13').length > 0 ){
		$('#bsDatepicker13').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker14').length > 0 ){
		$('#bsDatepicker14').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker15').length > 0 ){
		$('#bsDatepicker15').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

	if(jQuery('#bsDatepicker16').length > 0 ){
		$('#bsDatepicker16').datepicker({
			format: 'mm/dd/yyyy',
			autoclose: true,
			todayHighlight: true
		});
	}

});